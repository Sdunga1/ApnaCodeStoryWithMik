
  # Create Post and Login UI

  This is a code bundle for Create Post and Login UI. The original project is available at https://www.figma.com/design/W4mXPJrg9eNSK4YFhhXhij/Create-Post-and-Login-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  